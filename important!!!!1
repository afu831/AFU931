                else
                    booms.remove(i);
            }
            for (int j = 0; j < items.size(); j++) {
                if (!items.get(j).isLive()) {
                    items.remove(j);
                    continue;
                }
